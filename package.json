{
    "name": "botbuilder-tools",
    "scripts": {
        "test:travis": "nyc mocha \"@(Chatdown|LUIS|QnAMaker)*/test/*.test.suite.js\" --timeout 10000 && nyc report --reporter=text-lcov | coveralls",
        "eslint:travis": "eslint \"./**/*.js\""
    },
    "devDependencies": {
        "coveralls": "^3.0.2",
        "eslint": "^5.2.0",
        "mocha": "^5.2.0",
        "nyc": "^12.0.2"
    }
}
